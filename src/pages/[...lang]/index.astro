---
import Layout from '../../layouts/Layout.astro';
import Navigation from '../../components/Navigation.astro';
import Hero from '../../components/Hero.astro';
import Journey from '../../components/Journey.astro';
import Projects from '../../components/Projects.astro';
import Technologies from '../../components/Technologies.astro';
import Contact from '../../components/Contact.astro';
import Footer from '../../components/Footer.astro';
import FloatingContact from '../../components/FloatingContact.astro';

/**
 * Genera rutas estáticas para cada idioma soportado.
 * - undefined → / (español, idioma por defecto)
 * - 'en' → /en/
 */
export function getStaticPaths() {
  return [{ params: { lang: undefined } }, { params: { lang: 'en' } }];
}

type Lang = 'es' | 'en';
const lang: Lang = (Astro.params.lang as Lang) || 'es';

// Metadatos SEO por idioma
const seoData = {
  es: {
    title: 'Álvaro Lostal · Design Engineer',
    description:
      'Design Engineer en Santander. Ingeniería de software, diseño de producto digital y rendimiento web.',
  },
  en: {
    title: 'Álvaro Lostal · Design Engineer',
    description:
      'Design Engineer based in Santander. Software engineering, digital product design and web performance.',
  },
};

const { title, description } = seoData[lang];
---

<Layout lang={lang} title={title} description={description}>
  <div id="top"></div>
  <Navigation lang={lang} />
  <main id="main">
    <Hero lang={lang} />
    <Projects lang={lang} />
    <Journey lang={lang} />
    <Technologies lang={lang} />
    <Contact lang={lang} />
  </main>
  <Footer lang={lang} />
  <FloatingContact />
</Layout>
