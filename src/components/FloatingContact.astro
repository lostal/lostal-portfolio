---
export interface Props {
  lang: 'es' | 'en';
}
---

<div id="floating-contact" class="floating-contact">
  <script src="../scripts/floating-contact.ts"></script>
  <div class="floating-contact-inner">
    <a
      href="mailto:alvarolostal04@gmail.com"
      class="floating-contact-link"
      title="Email"
      data-contact="email"
    >
      <i class="fas fa-envelope"></i>
    </a>

    <a
      href="https://github.com/lostal"
      target="_blank"
      rel="noopener noreferrer"
      class="floating-contact-link"
      title="GitHub"
      data-contact="github"
    >
      <i class="fab fa-github"></i>
    </a>

    <a
      href="https://linkedin.com/in/alvarolostal"
      target="_blank"
      rel="noopener noreferrer"
      class="floating-contact-link"
      title="LinkedIn"
      data-contact="linkedin"
    >
      <i class="fab fa-linkedin"></i>
    </a>
  </div>
</div>

<style>
  /* === FLOATING CONTACT WIDGET - PEEK BEHAVIOR === */

  /* Widget base - posición centrada con peek */
  .floating-contact {
    position: fixed;
    bottom: -45px; /* Punto medio entre -32px y -42px = -37px, un poco más bajo */
    left: 50%;
    transform: translateX(-50%);
    z-index: 1000;
    transition: all 0.4s cubic-bezier(0.25, 0.46, 0.45, 0.94);
    pointer-events: none;
    opacity: 0;
    visibility: hidden;
  }

  /* Widget visible con peek */
  .floating-contact.visible {
    opacity: 1;
    visibility: visible;
    pointer-events: auto;
    bottom: -37px; /* Punto medio ajustado para mostrar peek perfecto */
  }

  /* Hover: widget completamente visible con elevación sutil */
  .floating-contact.visible:hover {
    bottom: 10px; /* Reducido de 24px para elevación más sutil */
  }

  /* Contenedor interno con glassmorphism - más compacto */
  .floating-contact-inner {
    display: flex;
    align-items: center;
    gap: 14px; /* Mismo gap que la sección de contacto */
    padding: 12px 20px; /* Más padding horizontal para hacer el widget más largo por los lados */
    background: rgba(var(--bg-primary-rgb), 0.95);
    backdrop-filter: blur(20px);
    border: 1px solid rgba(var(--border-rgb, 229, 231, 235), 0.3);
    border-radius: 36px; /* Reducido de 40px para ser más compacto */
    box-shadow:
      0 8px 32px rgba(0, 0, 0, 0.12),
      0 2px 16px rgba(0, 0, 0, 0.04),
      inset 0 1px 0 rgba(255, 255, 255, 0.1);
    transition: all 0.3s ease;
    position: relative;
  }

  /* Área invisible para hover estable - ANTI TEMBLOR */
  .floating-contact-inner::after {
    content: '';
    position: absolute;
    bottom: -40px;
    left: -20px;
    right: -20px;
    height: 50px;
    background: transparent;
    pointer-events: auto;
  }

  /* Hover elevado del contenedor */
  .floating-contact.visible:hover .floating-contact-inner {
    transform: translateY(-4px);
    box-shadow:
      0 16px 48px rgba(0, 0, 0, 0.15),
      0 4px 24px rgba(0, 0, 0, 0.08),
      inset 0 1px 0 rgba(255, 255, 255, 0.15);
  }

  /* Enlaces de contacto - IDÉNTICOS a la sección de contacto */
  .floating-contact-link {
    display: inline-flex;
    align-items: center;
    justify-content: center;
    width: 48px; /* Mismo tamaño que contact-link */
    height: 48px; /* Mismo tamaño que contact-link */
    background: var(--bg-secondary); /* Mismo background */
    border-radius: 24px; /* Mismo border-radius */
    color: var(--text-primary); /* Mismo color */
    text-decoration: none;
    font-weight: 500; /* Mismo font-weight */
    transition: all 0.3s ease; /* Misma transición */
    border: 1px solid var(--border); /* Mismo borde */
    font-size: 20px; /* Mismo font-size */
  }

  /* Hover idéntico a la sección de contacto */
  .floating-contact-link:hover {
    background: var(--accent-primary); /* Mismo hover background */
    color: #000; /* Mismo hover color */
    transform: translateY(-4px); /* Mismo hover transform */
    box-shadow: 0 12px 32px rgba(var(--accent-primary-rgb), 0.3); /* Misma sombra */
    border-color: var(--accent-primary); /* Mismo hover border */
  }

  /* Animaciones de deslizamiento */
  .floating-contact.slide-down {
    animation: slideDown 0.4s cubic-bezier(0.25, 0.46, 0.45, 0.94) forwards;
  }

  .floating-contact.slide-up {
    animation: slideUp 0.5s cubic-bezier(0.25, 0.46, 0.45, 0.94) forwards;
  }

  @keyframes slideDown {
    from {
      bottom: -37px;
      opacity: 1;
    }
    to {
      bottom: -100px;
      opacity: 0;
    }
  }

  @keyframes slideUp {
    from {
      bottom: -100px;
      opacity: 0;
    }
    to {
      bottom: -37px;
      opacity: 1;
    }
  }

  /* Ocultar en móviles */
  @media (max-width: 768px), (orientation: portrait), (pointer: coarse) {
    .floating-contact {
      display: none !important;
    }
  }

  /* Tema oscuro */
  @media (prefers-color-scheme: dark) {
    :global(:root:not([data-theme='light'])) .floating-contact-inner {
      background: rgba(var(--bg-primary-rgb), 0.95);
      border: 1px solid rgba(var(--border-rgb, 66, 66, 69), 0.5);
      box-shadow:
        0 8px 32px rgba(0, 0, 0, 0.4),
        0 2px 16px rgba(0, 0, 0, 0.3),
        inset 0 1px 0 rgba(255, 255, 255, 0.1);
    }
  }
</style>
